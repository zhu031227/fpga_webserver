# 任务进度汇报（续）—— 2026-09-02

> 接续：`whitelist_dev_v0011` 分支《任务进度汇报.md》（2026-09-01，理解+仿真验证六项全完成）
> 工程：fpga_webserver（ACX750 / XC7A35T-FGG484）· 分支基线：whitelist_dev @ `701944b`
> 本篇只汇报 09-01 之后新增完成的事项：干了什么 / 有没有成功 / 成果是什么。

---

## 一、事项汇报

### 事项 1：《跑版本手册》成文入库

| 干了什么 | 有没有成功 | 成果是什么 |
|----------|------------|------------|
| 把"从源码到板上"的人肉跑版本流程（仿真门禁→固件→bit→烧录→加载→板测→收尾）沉淀成手册；逐条对照仓内脚本实物核对后入仓、提交、推送 | ✅ 成功 | **`doc/跑版本手册.md`**（136 行：8 条快速通道 + 板测五项 + JTAG 排障三板斧 + 8 行坑表）；commit **`701944b`**；已推 zhu 双分支（`whitelist_dev` 与 `main` 均到 `701944b`） |

验证依据：手册每条命令均与仓内实物核对过（build/tcl 脚本用法注释、`jflow.tcl` 六个读数、`c_build` make 参数）；推送后远端双分支指针确认。

### 事项 2：L1/L2 带波形仿真链路打通

| 干了什么 | 有没有成功 | 成果是什么 |
|----------|------------|------------|
| 两个 TB 均无 `$dumpvars` 且不许改法官 → 在 `sim/` 新增**伴随 dump 顶层**（零侵入）→ 双 `-s` 编译 → VCD → GTKWave | ✅ 成功 | **`sim/wl_dump_l1.v`、`sim/wl_dump_l2.v`**（可复用，一行不改 TB 即可带波形重跑）；**L1 8/8 PASS（含 T7 cyc=18）**、**L2 7/7 PASS（含 case600）**；波形 `sim/tb_mac_whitelist_seq_iverilog.vcd`（89K）、`sim/tb_wl_integration_iverilog.vcd`（359K）已生成并打开 |

验证依据：两仿真均 `$finish` 正常退出（8.464ms / 21.52ms），无 FAIL 无挂起；首次编译踩"共享库内嵌自测 tb 当顶层"坑即报错退出，双 `-s` 复跑即过。

### 事项 3：对 09-01 汇报做了一次口径核查

| 干了什么 | 有没有成功 | 成果是什么 |
|----------|------------|------------|
| 核查昨日汇报技术表述与实证的一致性 | ⚠️ 发现 2 处不符 | 勘误口径：①"命中即提前退出"→ 实为**固定 18 拍**（T7 断言 hit/miss 均 cyc=18）；②"配置写后切换副本"→ 实为影子寄存器堆（回读）与主 BRAM（查找）**同时写**，无乒乓。后续文档以三图+TB 实证为准 |

## 二、未完成

- 教学主线「从 0 走一遍」：Q2 未交卷、手写 A/B/C 块未动笔——**无成果**，为下日第一件事。

## 三、待办

- 本汇报与 `sim/wl_dump_*.v` 尚未推送（分支选择待定）；三图 md/html 修改仍按原计划等 A 块打包。

---

*与《跑版本手册.md》《项目状态.md》配套查阅。*
