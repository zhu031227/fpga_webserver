# fpga_webserver · MAC 白名单项目状态

> 更新：2026-08-30 · 工程：`fpga_webserver-whitelist_dev_test`

## 项目主线

按《[MAC白名单查找引擎实现指南_模式0_顺序查找](MAC白名单查找引擎实现指南_模式0_顺序查找.md)》实施，主线为 **模式 0 → 模式 2（布谷鸟哈希）**，模式 1 为备选。

| 阶段 | 内容 | 状态 |
|------|------|------|
| 模式 0 | 顺序查找引擎 + 配置通路 + C 驱动 + L1/L2 仿真 | RTL/仿真完成，**上板待做** |
| 模式 2（主线后继） | 布谷鸟哈希查找 | 未开始 |
| 模式 1（备选） | 二分查找 | 未开始 |

## 模式 0 完成情况

### 门禁（Gate）

| Gate | 内容 | 状态 |
|------|------|------|
| G1 | 编译干净（L1/L2 依赖全编译） | ✅ |
| G2 | L1 单元 tb 8 用例全 PASS（含 ==18 拍） | ✅ |
| G3 | L2 集成 tb 6 用例全 PASS | ✅ |
| G4 | 产物齐套（.bit + InstructRAM.tcl） | ⏳ 待上板 |
| G5 | 板测 5 项 | ⏳ 待上板 |

### 交付物

| 文件 | 说明 |
|------|------|
| `rtl/mac_whitelist_seq.v` | 模式 0 查找引擎（参考实现，已存在） |
| `rtl/mac_whitelist_top.v` | LOOKUP_MODE 分发（=0 例化 seq） |
| `rtl/cpu_channel_tri.v` | 提取 SrcMAC + 触发查找 + push 门控（**2026-08-30 修复字节计数缺陷**） |
| `c/whitelist.c` | C 驱动（2026-08-30 加查重） |
| `sim/define.sv` | 仿真专用宏（新建） |
| `sim/tb_mac_whitelist_seq.sv` | L1 单元 tb（新建） |
| `sim/tb_wl_integration.sv` | L2 集成 tb（新建） |

## 关键记录

- **RTL 缺陷修复**：`cpu_channel_tri.v` eth1 头提取的 `mac1_byte_cnt` 在 SOP 拍被增量赋值覆盖，非首帧源 MAC 偏移 1 字节。修复：SOP 置 1 + `else if`。详见 [日报_20260830](日报_20260830.md)。
- **仿真 X 问题**：iverilog 未初始化 BRAM 为 X，miss 时 lookup_match 变 X，tb 内 DEL 全表初始化解决。

## 上板前置条件

1. 恢复共享库正本：`cp <v0001快照>/ip_common/rtl/define.sv ../ip_common/rtl/`（已知问题 9，当前工程外层无 `../ip_common`）。
2. `build_fpga.sh` 出 bitstream（半小时起），openFPGALoader 烧录 + `scripts/load_firmware_vivado.tcl` 加载固件。
3. 板测 5 项：`/api/wl/hwlist` 回读 / 过滤生效 / 拦截+drop_cnt / 全放 / 掉电持久化。

## 已知问题（待处理）

- `wl_status`(0x301) 无驱动：模式 0 读数恰为 0 可缓修，模式 2 必修。
- 板上免打流单次查表调试通道已就位：JTAG 写 `debug_wc_0` + MAC 寄存器可手动触发查找（已知问题 5）。
